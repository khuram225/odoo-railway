<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_aw_profile_section_list" model="ir.ui.view">
        <field name="name">aw.profile.section.list</field>
        <field name="model">aw.profile.section</field>
        <field name="arch" type="xml">
            <list>
                <field name="window_type_id"/>
                <field name="name"/>
            </list>
        </field>
    </record>

    <record id="view_aw_profile_section_form" model="ir.ui.view">
        <field name="name">aw.profile.section.form</field>
        <field name="model">aw.profile.section</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <group>
                            <field name="window_type_id"/>
                            <field name="name"/>
                        </group>
                    </group>
                    <field name="line_ids">
                        <list editable="bottom">
                            <field name="sequence" widget="handle"/>
                            <field name="position_id"/>
                            <field name="product_tmpl_id"/>
                            <field name="thickness_attribute_value_ids" column_invisible="1"/>
                            <field name="finish_attribute_value_ids" column_invisible="1"/>
                            <field name="thickness_id"/>
                            <field name="finish_id"/>
                            <field name="product_id" readonly="1"/>
                            <field name="is_optional"/>
                        </list>
                    </field>
                    <group>
                        <field name="notes" placeholder="Internal notes..."/>
                    </group>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="action_aw_profile_section" model="ir.actions.act_window">
        <field name="name">Profile Sections</field>
        <field name="res_model">aw.profile.section</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">Create your first Profile Section</p>
            <p>A Profile Section maps every needed position (Outer Frame, Palay,
               Mesh, Dividers, Fixed Bead — see Profile Positions) to a real
               profile product, for one Window Series.</p>
        </field>
    </record>

</odoo>
